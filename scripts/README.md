# Scripts

- `create_tree.sh` — bootstrap a task worktree from a branch (copies env files, sets upstream, installs deps with pnpm).

Add new scripts here with a one-line description and keep this list in sync with AGENTS.md.
